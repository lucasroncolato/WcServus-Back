-- noop migration
-- This migration folder duplicated the SQL from
-- 20260327_full_ministry_domain_migration. Keeping this as a no-op
-- preserves migration history order without replaying destructive renames.
SELECT 1;
